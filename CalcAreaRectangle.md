# Atividade 01 - Calcular área com Python 🐍
```
def Main():
    width = float(input("Digite a largura do retângulo: "))
    height = float(input("Digite a altura do retângulo: "))
    area = width * height
    print(f"A área do retângulo é: {area}")

if __name__ == "__main__":
  Main()
```
